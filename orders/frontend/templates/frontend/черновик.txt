
1. Исправления в шаблонах (исправлены конструкции if):
products.html

Apply
# Было:
{{ product.product.category.name if product.product.category }}

# Стало:
{% if product.product.category %}
    {{ product.product.category.name }}
{% endif %}
product_detail.html

Apply
# Было:
{{ product.product.category.name if product.product.category }}

# Стало:
{% if product.product.category %}
    {{ product.product.category.name }}
{% endif %}
checkout.html

Apply
# Было:
{{ product.product.category.name if product.product.category }}

# Стало:
{% if product.product.category %}
    {{ product.product.category.name }}
{% endif %}
orders.html

Apply
# Было:
{{ order.contact.city }}, {{ order.contact.street }}, {{ order.contact.house }}<br>
{{ order.contact.phone }}

# Стало:
{% if order.contact.city %}{{ order.contact.city }}{% endif %},
{% if order.contact.street %}{{ order.contact.street }}{% endif %},
{% if order.contact.house %}{{ order.contact.house }}{% endif %}<br>
{% if order.contact.phone %}{{ order.contact.phone }}{% endif %}
2. Исправления в base.html (добавлен тег {% load static %}):

Apply
# Было:
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Автоматизация закупок{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="{% static 'frontend/css/style.css' %}" rel="stylesheet">
</head>

# Стало:
{% load static %}
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Автоматизация закупок{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="{% static 'frontend/css/style.css' %}" rel="stylesheet">
</head>
3. Исправления в orders.html (исправлен пагинатор):

Apply
# Было:
{% for num in orders.paginator.page_range %}
    {% if orders.number == num %}
    <li class="page-item active">
        <a class="page-link" href="?page={{ num }}">{{ num }}</a>
    </li>
    {% elif num > orders.number|add:'-3' and num < orders.number|add:'3' %}
    <li class="page-item">
        <a class="page-link" href="?page={{ num }}">{{ num }}</a>
    </li>
    {% endif %}
{% endfor %}

# Стало:
{% for num in orders.paginator.page_range %}
    {% if orders.number == num %}
    <li class="page-item active">
        <a class="page-link" href="?page={{ num }}">{{ num }}</a>
    </li>
    {% elif num > orders.number|add:'-3' and num < orders.number|add:'3' %}
    <li class="page-item">
        <a class="page-link" href="?page={{ num }}">{{ num }}</a>
    </li>
    {% endif %}
{% endfor %}
4. Исправления в cart.html (исправлены ссылки на изображения):

Apply
# Было:
<img src="{{ item.product_info.product.image.url if item.product_info.product.image else '/static/frontend/images/placeholder.jpg' }}"
     alt="{{ item.product_info.product.name }}" class="me-3" style="width: 60px; height: 60px; object-fit: cover;">

# Стало:
<img src="{% if item.product_info.product.image %}{{ item.product_info.product.image.url }}{% else %}/static/frontend/images/placeholder.jpg{% endif %}"
     alt="{{ item.product_info.product.name }}" class="me-3" style="width: 60px; height: 60px; object-fit: cover;">
5. Исправления в order_detail.html (исправлены ссылки на изображения):

Apply
# Было:
<img src="{{ item.product_info.product.image.url if item.product_info.product.image else '/static/frontend/images/placeholder.jpg' }}"
     alt="{{ item.product_info.product.name }}" class="me-3" style="width: 50px; height: 50px; object-fit: cover;">

# Стало:
<img src="{% if item.product_info.product.image %}{{ item.product_info.product.image.url }}{% else %}/static/frontend/images/placeholder.jpg{% endif %}"
     alt="{{ item.product_info.product.name }}" class="me-3" style="width: 50px; height: 50px; object-fit: cover;">
6. Исправления в orders.html (исправлены поля контакта):

Apply
# Было:
{{ order.contact.city }}, {{ order.contact.street }}, {{ order.contact.house }}<br>
{{ order.contact.phone }}

# Стало:
{% if order.contact.city %}{{ order.contact.city }}{% endif %},
{% if order.contact.street %}{{ order.contact.street }}{% endif %},
{% if order.contact.house %}{{ order.contact.house }}{% endif %}<br>
{% if order.contact.phone %}{{ order.contact.phone }}{% endif %}
Все эти исправления направлены на:

Исправление синтаксиса - замена {{ ... if ... }} на {% if %}...{% endif %}
Предотвращение ошибок - проверка существования полей перед их выводом
Обеспечение безопасности - предотвращение ошибок при отсутствии данных
Теперь все страницы должны работать без ошибок TemplateSyntaxError.